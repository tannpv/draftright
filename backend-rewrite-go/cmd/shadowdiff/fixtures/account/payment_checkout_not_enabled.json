{
  "name": "account_payment_checkout_not_enabled",
  "method": "POST",
  "path": "/payment/checkout",
  "headers": {
    "Authorization": "Bearer {{user_token}}",
    "Content-Type": "application/json"
  },
  "body": { "plan_id": "00000000-0000-4000-8000-000000000004", "method": "paypal" },
  "_note": "PayPal is enabled in the shadow env, so both backends return a real checkout; the value-ignored keys below are per-request random / timestamp fields (payment id/codes/times + the PayPal ba_token embedded in redirect_url). Presence + non-emptiness are still asserted. If the intent was to exercise the DISABLED path, disable PayPal in the shadow env (augment.sql) instead — see #194.",
  "ignore_value_of": [
    "request_id",
    "id", "created_at", "updated_at", "expires_at",
    "reference_code", "redirect_url"
  ]
}
