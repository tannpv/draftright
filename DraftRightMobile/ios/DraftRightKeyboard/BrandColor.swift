import UIKit

extension UIColor {
    /// DraftRight brand blue — matches the website primary, rgb(93 135 255).
    static let draftRightBrand = UIColor(red: 93.0 / 255.0,
                                         green: 135.0 / 255.0,
                                         blue: 255.0 / 255.0,
                                         alpha: 1.0)
}
