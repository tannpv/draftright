app-id: com.draftright.app
runtime: org.gnome.Platform
runtime-version: '46'
sdk: org.gnome.Sdk
command: draftright

finish-args:
  # Network access for API calls
  - --share=network
  # X11 display
  - --share=ipc
  - --socket=x11
  # Wayland display
  - --socket=wayland
  # System tray (StatusNotifierItem, served by the GTK3 tray helper).
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=org.freedesktop.Notifications
  # NOTE: the Wayland global shortcut (#99) needs no talk-name — portals are
  # always reachable from inside the sandbox, and org.freedesktop.portal.
  # GlobalShortcuts is an *interface* on org.freedesktop.portal.Desktop, not a
  # bus name, so the previous --talk-name for it never granted anything.
  # A sandboxed app gets its app id for free, so the portal will not reject it
  # the way an unsandboxed launch does.

modules:
  - name: python3-requests
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
    sources:
      - type: archive
        url: https://files.pythonhosted.org/packages/63/70/2bf7780ad2d390a8d301ad0b550f1581eadbd9a20f896afe06353c2a2913/requests-2.32.3.tar.gz
        sha256: 55365417734eb18255590a9ff9eb97e9e1da868d4ccd6402399eaf68af20a760

  - name: python3-urllib3
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
    sources:
      - type: archive
        url: https://files.pythonhosted.org/packages/aa/63/e53da845320b757bf29ef6a9062f5c669571d2c2f01d3a578a20e1e1c8c1/urllib3-2.2.3.tar.gz
        sha256: e7d814a81dad81e6caf2ec9fdedb284ecc9c73076b62654547cc64ccdcae26e9

  - name: python3-certifi
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
    sources:
      - type: archive
        url: https://files.pythonhosted.org/packages/b0/ee/9b19140fe824b367c04c5e1b369942dd754c4c5462d5674002f75c4dedc1/certifi-2024.8.30.tar.gz
        sha256: bec941d2aa8195e248a60b31ff9f0558284cf01a52591ceda73ea9afffd69fd9

  - name: python3-charset-normalizer
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
    sources:
      - type: archive
        url: https://files.pythonhosted.org/packages/63/09/c1bc53dab74b1816a00d8d030de5bf98f724c52c1635e07681d312f20be8/charset-normalizer-3.3.2.tar.gz
        sha256: f30c3cb33b24454a82faecaf01b19c18562b1e89558fb6c56de4d9118a032fd5

  - name: python3-idna
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
    sources:
      - type: archive
        url: https://files.pythonhosted.org/packages/bf/3f/ea4b9117521a1e9c50344b909be7886dd00a519552724809bb1f486986c2/idna-3.6.tar.gz
        sha256: 9ecdbbd083b06798ae1e86adcbfe8ab1479cf864e4ee30fe4e46a003d12491ca

  # python-xlib powers the X11 global hotkey (XGrabKey). It was missing here,
  # so a Flatpak running on X11 started cleanly and simply had no hotkey.
  # `six` is a hard runtime import of Xlib (display.py, protocol/rq.py), not
  # just a build dep, so it has to ship too.
  - name: python3-six
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
    sources:
      - type: archive
        url: https://files.pythonhosted.org/packages/94/e7/b2c673351809dca68a0e064b6af791aa332cf192da575fd474ed7d6f16a2/six-1.17.0.tar.gz
        sha256: ff70335d468e7eb6ec65b95b99d3a2836546063f63acc5171de367e834932a81

  - name: python3-xlib
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
    sources:
      - type: archive
        url: https://files.pythonhosted.org/packages/86/f5/8c0653e5bb54e0cbdfe27bf32d41f27bc4e12faa8742778c17f2a71be2c0/python-xlib-0.33.tar.gz
        sha256: 55af7906a2c75ce6cb280a584776080602444f75815a7aff4d287bb2d7018b32

  # The tray indicator runs as a separate GTK3 process (draftright.tray_helper)
  # because Ayatana AppIndicator cannot be loaded beside GTK4.  org.gnome.
  # Platform ships GTK3, but NOT libayatana-appindicator, so the tray stays
  # disabled in Flatpak until this library is built here as a module.  The app
  # degrades cleanly without it — only the tray icon is missing.
  # TODO(#93): add a libayatana-appindicator module to restore the tray.

  - name: draftright
    buildsystem: simple
    build-commands:
      - pip3 install --no-build-isolation --prefix=/app --no-deps .
      - install -Dm644 data/com.draftright.app.desktop /app/share/applications/com.draftright.app.desktop
      - install -Dm644 data/com.draftright.app.metainfo.xml /app/share/metainfo/com.draftright.app.metainfo.xml
      # App icon: without these the shell shows a generic fallback for the
      # desktop entry's Icon=com.draftright.app.
      # Symbolic variant for the top bar (recoloured by the shell).
      - install -Dm644 data/icons/hicolor/scalable/apps/com.draftright.app-symbolic.svg
          /app/share/icons/hicolor/scalable/apps/com.draftright.app-symbolic.svg
      - |
        for size in 16 24 32 48 64 128 256 512; do
          install -Dm644 "data/icons/hicolor/${size}x${size}/apps/com.draftright.app.png" \
            "/app/share/icons/hicolor/${size}x${size}/apps/com.draftright.app.png"
        done
    sources:
      - type: dir
        path: ../..
