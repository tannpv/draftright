PyGObject>=3.44.0
pycairo>=1.24.0
requests>=2.31.0
# Needed for the X11 global hotkey (XGrabKey). Without it the app starts and
# looks fine but has no hotkey at all on X11 — the failure is a single log
# line. Harmless on Wayland, which uses the portal instead.
python-xlib>=0.33
