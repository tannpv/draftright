import SwiftUI

/// Hosts [SubscriptionView] inside the Settings TabView so the user
/// can manage their plan without leaving the Settings window.
struct SubscriptionSettingsTab: View {
    var body: some View {
        SubscriptionView()
    }
}
